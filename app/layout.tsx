import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Navbar from '@/components/Navbar'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'NexVision Technologies - AI & Embedded Systems Expert',
  description: 'Leading expert in Artificial Intelligence, Computer Vision, Embedded Systems, and Construction Management Solutions',
  keywords: 'AI, Machine Learning, Computer Vision, YOLO, Embedded Systems, BLDC, Construction Management, ROS2, ADAS',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}


